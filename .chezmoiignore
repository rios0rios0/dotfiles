# Common
LICENSE
README.md

# Windows
{{ if ne .chezmoi.os "windows" }}
  .aws
  .azure
  .ssh # TODO: it's missing a lot of files
  AppData
  .oh-my-posh.json
  .wakatime.cfg
  copy-app-data-files.ps1
  create-ssh-public-keys.ps1
  export-private-key.ps1
{{ end }}

# Linux
{{ if ne .chezmoi.os "linux" }}
  .docker
  .kube
  .autobump.yaml
  .freterc
  .npmrc
  .npmrc.2
  .p10k.zsh
  .zshrc
  export-private-key.sh
  extract-kube-config-files.sh
  pyvenv.cfg # TODO: do I really need this file?
  watch-kube-config-files.sh
{{ end }}
