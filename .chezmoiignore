# Common
LICENSE
README.md

# Windows
{{- if eq .chezmoi.os "windows" }}
  .docker
  .kube
  .freterc
  .p10k.zsh
  .zshrc
  # TODO: do I really need this file?
  pyvenv.cfg
{{- end }}

# Linux
{{- if ne .chezmoi.os "windows" }}
  .aws
  .azure
  .ssh
  .oh-my-posh.json
  .wakatime.cfg
{{- end }}
