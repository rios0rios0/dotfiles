# TODO:
0 - change to place the private key ssh inside the folder .ssh and not in chezmoi config folder
1 - export all active PEMs to the .ssh folder
2 - export all active GPGs to the .gpg folder + import them in the keyring
2 - check how to avoid 1Password duplicated calls
3 - check how to use variables from a included template
