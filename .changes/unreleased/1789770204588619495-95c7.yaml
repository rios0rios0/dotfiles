kind: 'Added'
body: 'added a WSL SSH agent bridge exposing the Windows 1Password agent as a Unix socket at `~/.ssh/agent.sock`, so Go-based tools that dial a socket instead of running `ssh` can reach the keys'
time: '2026-09-18T22:23:24.588520668Z'
