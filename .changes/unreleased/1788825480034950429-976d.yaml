kind: 'Changed'
body: 'refreshed `.github/skills/code-review/SKILL.md` and `.github/copilot-instructions.md` to match the current codebase: reworded the secret-hygiene bullet in the `code-review` skill to name the vendor behind each credential prefix and to drop the hyphen from the Slack example, the one shape the shared Gitleaks history scan matches on its own, added the `test-clipboard-shim` target to the `make test` summary, and added the `clipshot` logging prefix'
time: '2026-09-07T23:58:00.034855064Z'
