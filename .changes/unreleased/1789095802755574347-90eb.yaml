kind: 'Added'
body: 'added `make test-nvm-resolution`, covering NVM default-alias resolution in `dot_zshenv.tmpl` — the alias chain past a newer Current release, a bare version number, a target that is no longer installed, a partial version alias and one that must not match a longer major, the `system` alias, an alias cycle, and no alias at all'
time: '2026-09-11T03:03:22.755506394Z'
