{{- $deviceName := .deviceName -}}

{{- warnf “[authorized-keys] fetching Active SSHs...\n” -}}
{{- range (onepassword “Active SSHs” “personal” “my”).fields -}}
  {{- if (eq .type “REFERENCE”) -}}

    {{- $item := onepassword .value “Private” “my” -}}
    {{- warnf “[authorized-keys] checking SSH item \”%s\”...\n” $item.title -}}

    {{- /* Split title into [device, account] */ -}}
    {{- $parts := split “@” $item.title -}}
    {{- $device := index $parts “_0” -}}

    {{- /* Only emit hosts for the current device */ -}}
    {{- if eq $device $deviceName -}}
      {{- warnf “[authorized-keys] matched device \”%s\”\n” $deviceName -}}

      {{- $f := dict -}}
      {{- range $item.fields -}}
        {{- $_ := set $f .label .value -}}
      {{- end -}}
      {{- printf “%s\n” (index $f “public key”) -}}

    {{- end -}}
  {{- end -}}
{{- end }}
